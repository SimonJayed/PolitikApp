# Active Profile - Defaults to local H2, or 'supabase' via CLI/environment
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}

# General Application Properties
server.port=8080
spring.application.name=backend