# Active Profile - Defaults to Supabase Cloud, or 'local' via CLI/environment
spring.profiles.active=${SPRING_PROFILES_ACTIVE:supabase}

# General Application Properties
server.port=8080
spring.application.name=backend